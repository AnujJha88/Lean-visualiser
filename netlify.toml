[build]
base = "frontend"
publish = "dist"
command = "npm install && npm run build"

[dev]
command = "npm run dev"
port = 5173
publish = "dist"

# Redirect all routes to index.html for SPA to handle routing
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
